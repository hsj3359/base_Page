from django.db import models

'''
join - p / group(f), user(f), host(true/false), level, exp
schedule - name / date / contents / join.p(f) / performance(true / false)
subject - name / date / contents / join.p(f) / performance(true / false), score
'''
